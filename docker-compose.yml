version: "3.8"

services:
  app:
    image: devdanilboe/devops-for-developers-project-74:latest
    build:
      context: .
      dockerfile: Dockerfile.production
    working_dir: /app
    command: make test
