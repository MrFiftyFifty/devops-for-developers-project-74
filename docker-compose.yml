version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    volumes:
      - ./app:/app
    user: "${UID:-1000}:${GID:-1000}"
    command: make test
